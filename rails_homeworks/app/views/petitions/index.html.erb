<center><h2><%= params[:my] ? "Мои" : "" %> петиции</h2></center>

<table>
  <thead>
    <tr>
      <th>Петиции</th>
      <th>Автор</th>
    </tr>
  </thead>

  <% @petitions.each do |petition| %>
    <%= render 'petition', petition: petition %>
  <% end %>
</table>
